# AI 工作猎手本机后端

## 第一次使用

1. 启动 Docker Desktop，等待左下角显示 Docker Engine 正常运行。
2. 打开 `.env`，填写：
   - `AI_API_KEY`：通义千问 DashScope 或其他兼容 OpenAI API 的 Key。
   - `AI_BASE_URL`、`AI_MODEL`：示例默认使用 `qwen3-vl-32b-thinking`。
   - `KIMI_API_KEY`：导入并解析 PDF 简历时需要。
3. 右键 `start-job-helper.ps1`，选择“使用 PowerShell 运行”。
4. 第一次启动需要下载镜像和 Maven 依赖，时间可能较长。
5. 在油猴脚本的“服务器配置”中填写：

   `http://127.0.0.1:9100/`

启动脚本也会在后台启动本机前端：`http://127.0.0.1:5173/`。新版油猴脚本位于
`ai-job-hunting-local.user.js`，安装后打开 BOSS 职位页会自动使用真实、不限量模式开始投递。

## 常用操作

- 启动或更新：`start-job-helper.ps1`
- 查看状态和日志：`status-job-helper.ps1`
- 运行完整只读诊断：`debug-job-helper.ps1 -Action all`
- 运行一次轻量监测：`monitor-job-helper.ps1`
- 停止：`stop-job-helper.ps1`

数据保存在 Docker 卷中，执行普通的停止或重启不会丢失。

## 注意

- 后端和数据库端口仅绑定 `127.0.0.1`，局域网和互联网无法访问。
- 自动回复仍要求 Docker Desktop运行，并保持 BOSS 网页登录和脚本运行。
- 简历和聊天内容不会发送给原作者服务器，但会发送给你配置的 AI 服务商。
