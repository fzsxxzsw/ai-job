# Job Helper 本机部署

目前使用一套本地 Python 服务：业务 API `127.0.0.1:9100`、LangGraph Agent `127.0.0.1:9101`、MySQL，以及兼容静态服务 `127.0.0.1:5173`。Chrome 仍连接 9100。Java 源码保留供历史对照，正常构建不再使用 Java、Maven 或 6768 管理端口。

用户在 2026-09-07 明确采用“先本地构建和验收，再提交推送 GitHub”的顺序。日常命令如下：

```powershell
.\build-job-helper.ps1
.\release-job-helper.ps1
.\status-job-helper.ps1
# 后续关闭/启动当前版本：
.\stop-job-helper.ps1
.\start-job-helper.ps1
```

build 使用冻结的 Python 锁文件和 pnpm 9.15.9，执行 API、Agent、UI、类型、MV3 安全合同和维护脚本测试，再构建两个 Python 镜像。它不启动服务、不迁移数据库、不改 Chrome 安装目录。构建期间源代码变化会中止构建。

release 只发布已通过 build 的同一源码快照。未提交源码由 HEAD 加 diff SHA-256 标识；发布后提交到 Git 的源码须与验收内容一致，并在提交说明中记录 build ID。GitHub Actions 作为远端验证与版本档案，不是本地发布的前置门禁。

## 只有一个 Chrome 发布目录

Chrome 现有扩展始终从源码目录同级的 `job-helper-wxt-local-extension` 加载，本机完整路径是 `C:\Users\Administrator\Desktop\job-helper-wxt-local-extension`。重建覆盖这个目录，保持扩展 ID 和已有存储。不要另建日期目录，不要重新加载另一个扩展副本。

- `.job-helper-builds/candidate/`：唯一未发布候选，下一次 build 覆盖。
- `.job-helper-releases/current/`：当前回执和归档。
- `.job-helper-releases/previous/`：仅一份前版退路。
- `.job-helper-active.json`：唯一当前指针。
- `job-helper-backend-rollback`、`job-helper-agent-rollback`：上一版停止的容器，保留原运行参数；下次发布替换更旧退路。

回执记录扩展版本、build ID、源码身份、每个镜像的精确 image ID 和扩展文件树 SHA-256。可变镜像标签仅作便利别名，不作为启动验证依据。API 健康、Agent 健康、扩展 manifest 与静态 runtime 必须匹配同一版本/build ID。

文件同步不代表 Chrome 已加载新代码。浏览器刷新、扩展重载和页面徽标核验必须按浏览器安全约定单独取得具体授权。脚本本身不操纵 Chrome/BOSS，不发消息、不启动投递。

## 第一次从 Java 切换

先在独立 MySQL 副本上运行兼容验收。正式 `.env` 的 `API_OWNER_USER_ID` 要绑定原账号；显式迁移在新鲜备份之后只暂停该有效账号的 AI 回复，并记录先前状态。历史会话也会保守暂停，切换成功或回滚都不会自动恢复发送，重新启用须由用户主动操作。新安装 `API_CONFIRMED_EDUCATION` 默认为空，本机迁移可保留原来明确配置的学历值，不能把开发者身份写为默认值。

正式切换时执行：

```powershell
.\build-job-helper.ps1
.\release-job-helper.ps1 -ApplyMigrations
```

只有 `-ApplyMigrations` 才执行数据库变更：先停止原 backend/agent writer，做完整 MySQL dump，核对完成标记与 SHA-256，再运行 Agent Alembic 与 Python API 显式迁移。备份固定在 `.job-helper-releases/database-before.sql`，避免每次创建新的大体积目录。若指定 `-DatabaseBackupPath`，它是新备份的输出位置，必须尚不存在；脚本仍会在停写后重新导出，不能把早先诊断副本作为迁移凭据。

迁移失败会恢复原容器；启动或身份检查失败会恢复扩展/静态文件和原容器。`.job-helper-releases/last-migration.json` 保留最近备份哈希、迁移结果和账号 AI 回复原状态。数据库只做兼容迁移，失败后不自动覆盖用户数据或恢复自动回复。恢复数据库必须人工核对备份和变更。

若断电或进程中断留下 `activation.json`，普通 start 会拒绝继续。检查运行情况后执行 `release-job-helper.ps1 -Recover`，恢复精确旧容器和发布文件。不要用 `docker compose up/down` 绕过当前指针或回退容器。

## 验证和 Git 归档

本地完整验收应覆盖原扩展 API 合同、真实数据副本迁移、模型失败降级、拒绝报告保存/反馈/用户隔离、停止后启动、一次失败发布回滚。健康检查和模拟模型测试不能冒充真实页面/模型验收。静态 5173 服务用于兼容，不是另一个扩展安装版本。

验收通过后只提交本次授权的源码，先运行 GitNexus `detect_changes(scope=all)` 并确认无 partial/truncated，再提交推送，等待 GitHub 四类 Python/UI/维护验证结果。版本长期历史由 Git 管理。

清理只应删除经核对的旧发布目录和本项目旧 image 标签；保护 current/previous、运行中的其他项目、MySQL/Agent/API 数据卷及所有未提交源码。不得使用全局 `docker system prune`、`volume prune` 或按模糊通配符删除桌面目录。
