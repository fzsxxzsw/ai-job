# AI 工作猎手本机部署

当前本机部署由四个明确分开的入口维护，避免一次“启动”同时执行测试、构建和发布：

| 命令 | 职责 | 不会做什么 |
| --- | --- | --- |
| `build-job-helper.ps1` | 前端/Python/Java 测试、bundle、构建带 SHA 标签的候选镜像 | 不启动服务、不发布运行时、不覆盖正式镜像标签 |
| `release-job-helper.ps1` | 校验同一提交的三项指定 CI，验证不可变候选镜像/runtime 后原子切换激活版本 | 默认不允许脏工作区、未推送或指定 CI 缺失/失败的版本 |
| `start-job-helper.ps1` | 按当前激活指针和不可变回执校验四个镜像、image ID、SHA/build ID 与 runtime 后启动 | 不测试、不构建、不拉取镜像、不发布运行时 |
| `status-job-helper.ps1` | 查看前端、Java 和 Python Agent 的只读健康状态 | 默认不打印服务日志 |

## 第一次使用

1. 启动 Docker Desktop，等待 Docker Engine 就绪。
2. 从 `.env.example` 复制 `.env`，填写数据库密码与 AI 配置。
3. 将预期改动提交并推送到 `fork` 的同名分支，等待对应 GitHub Actions 全部成功。
4. 运行：

   ```powershell
   .\release-job-helper.ps1
   ```

5. 后续普通重启只运行：

   ```powershell
   .\start-job-helper.ps1
   ```

需要明确绕过推送和 CI 门禁做本机紧急预览时，必须显式运行
`.\release-job-helper.ps1 -EmergencyPreview`。紧急预览使用带工作区 diff identity 的独立 `emergency-*` 镜像和回执，绝不覆盖正式 `:release` 标签。验证成功后，当前激活指针会明确显示 `channel=emergency`，普通 start/status 会继续使用它，避免共享 runtime 与旧正式回执悄悄错配。

从紧急预览恢复正式版本时，先提交并推送最终改动、等待三项 CI 成功，再正常运行 `.\release-job-helper.ps1`（不要带 `-EmergencyPreview`）。只有新的正式候选镜像、健康检查和 served runtime 全部通过后，激活指针才切回 `channel=release`。

普通 build 生成带 source identity 和唯一 build ID 的 `candidate-*` 镜像。release 在 `UI build`、`Local runtime maintenance`、`Python Agent build` 三项工作流均针对同一 SHA 成功后，先用不可变候选 tag/image ID 启动验证；发布失败会恢复原 runtime，旧激活指针和旧 `:release` alias 保持不变。全部通过后才原子切换激活指针，随后 `:release` 仅作为可选便利 alias 更新，不作为启动依据。

每次 build 使用独立的 `.job-helper-builds/<buildId>.json`，不会覆盖其他构建回执。每次成功候选发布还会在 `.job-helper-releases/<buildId>/` 保存不可变回执以及 runtime、public loader、dist loader、根目录 loader 四个归档文件和各自 SHA-256。启动会核对 nginx、MySQL、Java、Agent 四个精确 image ID，不能仅凭可变 tag 通过。

build 完成全部测试和镜像构建后会重新计算 HEAD、dirty 状态、diff hash 和 source identity；任一项在构建期间改变都会终止且不写可发布回执。正式 release 在切换激活指针前还会再次验证源码仍为同一 clean 快照。

build/start/release 共用工作区互斥锁，另一个维护操作正在运行时会立即拒绝并给出提示。候选激活失败时，系统会先从旧 immutable receipt 的四文件归档恢复浏览器文件，再按旧回执恢复四个容器；没有旧版本时会移除候选 frontend、MySQL、Java、Agent 容器。若旧版本恢复也失败，会明确报告“候选失败 + 回滚失败”，不会显示发布成功。

## 服务边界

- 前端及已发布用户脚本运行时：`http://127.0.0.1:5173/`
- 原 Java API：`http://127.0.0.1:9100/`
- 新 Python Agent：`http://127.0.0.1:9101/`
- Smart Config：`http://127.0.0.1:6768/`

Python Agent 当前阶段只提供健康检查和只读岗位决策，不调用模型、Java、MySQL 或浏览器，也不会生成投递、沟通等动作。Checkpoint、人工暂停/恢复和动作 Outbox 属于后续阶段。

## 常用操作

```powershell
.\status-job-helper.ps1
.\debug-job-helper.ps1 -Action all
.\monitor-job-helper.ps1
.\stop-job-helper.ps1
```

普通停止和重启不会删除 Docker 数据卷。浏览器脚本或扩展的安装、刷新和页面验证均不是本机服务启动的一部分，需要单独明确操作。
